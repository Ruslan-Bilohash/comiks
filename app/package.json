{
  "name": "komiks-lab-app",
  "version": "1.0.0",
  "private": true,
  "description": "Комікс·Lab — мобільний застосунок (iOS + Android) на базі того самого сайту",
  "scripts": {
    "sync": "node tools/prepare-web.js && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios",
    "add:android": "npx cap add android",
    "add:ios": "npx cap add ios",
    "run:android": "npm run sync && npx cap run android",
    "run:ios": "npm run sync && npx cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/haptics": "^6.0.1",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/splash-screen": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
